name: ltogt_utils_flutter
description: A collection of utility widgets, helpers, ... for flutter
version: 0.0.1
homepage: https://github.com/ltOgt
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  ltogt_utils:
    path: /home/omni/repos/package/ltogt_utils

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: null
